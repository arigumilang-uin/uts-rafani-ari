<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="#FFFFFF"> <!-- Latar belakang putih -->

    <!-- Avatar dengan Border Bulat -->
    <ImageView
        android:id="@+id/tvUserAvatar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:background="@drawable/circular_border"
        android:src="@drawable/ic_default_avatar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"/>

    <!-- Tombol Ganti Avatar (Ikon Kamera) -->
    <ImageButton
        android:id="@+id/btnChangeAvatar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackground"
        android:src="@drawable/ic_camera"
        android:layout_below="@id/tvUserAvatar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>

    <!-- Nama Pengguna -->
    <TextView
        android:id="@+id/tvUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User Name"
        android:textColor="#000000"
    android:textSize="24sp"
    android:textStyle="bold"
    android:layout_below="@id/btnChangeAvatar"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="40dp"/>

    <!-- Tombol Logout -->
    <Button
        android:id="@+id/btnLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textStyle="bold"
        android:textColor="#000000"
    android:backgroundTint="#FFEB3B"
    android:layout_below="@id/tvUserName"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="40dp"
    android:padding="12dp"
    android:layout_marginBottom="40dp"/>

</RelativeLayout>
